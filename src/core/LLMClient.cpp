#include "core/LLMClient.h"

// Implementation file for LLMClient
// This is mostly an interface, but we can add common functionality here

// Base LLMClient is abstract, so no implementation needed for now
// Future utility functions can be added here 